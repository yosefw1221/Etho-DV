import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Etho-DV | DV Lottery Application Service',
  description: 'Professional DV lottery application service for Ethiopian users. Simple, fast, and reliable.',
  keywords: 'DV lottery, diversity visa, Ethiopia, immigration, US visa',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}